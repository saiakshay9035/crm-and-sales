version: '3.8'

services:
  ai-lead-platform:
    build: .
    ports:
      - "5050:5050"
    environment:
      - PORT=5050
      - DB_PATH=/app/leads.db
    volumes:
      - ./leads.db:/app/leads.db
    restart: unless-stopped
